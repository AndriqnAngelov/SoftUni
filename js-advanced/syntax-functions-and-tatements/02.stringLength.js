function solved(inputOne, inputTwo, inputThree) {
  let lengthOfInputs = Number(
    inputOne.length + inputTwo.length + inputThree.length
  );
  let averageLentgh = Math.floor(lengthOfInputs / 3);
  console.log(lengthOfInputs);
  console.log(averageLentgh);
}

